# CI pipeline for Azure DevOps. Equivalent to the old .github/workflows/ci.yml.
# npm ci → lint → compile (typecheck) → tests → package .vsix → publish artifact.

trigger:
  branches:
    include:
      - main

pr:
  branches:
    include:
      - main

pool:
  vmImage: ubuntu-latest

steps:
  - task: UseNode@1
    displayName: 'Install Node.js 20'
    inputs:
      version: '20.x'

  # Cache ~/.npm between runs (key = hash of package-lock.json).
  - task: Cache@2
    displayName: 'npm cache'
    inputs:
      key: 'npm | "$(Agent.OS)" | package-lock.json'
      restoreKeys: |
        npm | "$(Agent.OS)"
      path: $(Pipeline.Workspace)/.npm

  - script: npm config set cache "$(Pipeline.Workspace)/.npm" --global
    displayName: 'Point npm to the cache'

  - script: npm ci
    displayName: 'Install dependencies (npm ci)'

  - script: npm run lint
    displayName: 'Lint (ESLint)'

  - script: npm run compile
    displayName: 'Compile (typecheck)'

  - script: npm test
    displayName: 'Tests (node:test)'

  - script: npx --yes @vscode/vsce package -o lang-chat.vsix
    displayName: 'Package (.vsix)'

  - task: PublishPipelineArtifact@1
    displayName: 'Publish the .vsix'
    inputs:
      targetPath: lang-chat.vsix
      artifact: vsix
